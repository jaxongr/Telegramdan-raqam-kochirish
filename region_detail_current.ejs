<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= route.name %> - Telegram SMS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <style>
        .district-card {
            cursor: pointer;
            transition: all 0.2s;
        }
        .district-card:hover {
            background-color: #f8f9fa;
        }
        .stats-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 10px;
            padding: 20px;
        }
    </style>
</head>
<body>
    <%- include('../partials/sidebar') %>
    
    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
        <!-- Header -->
        <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3">
            <h1 class="h2">
                <a href="/routes/regions" class="text-decoration-none">
                    <i class="bi bi-arrow-left"></i>
                </a>
                <%= route.name %>
            </h1>
        </div>

        <!-- Statistika -->
        <div class="stats-header mb-4">
            <div class="row text-center">
                <div class="col-md-3">
                    <h3><%= totalPhones %></h3>
                    <p class="mb-0"><i class="bi bi-phone"></i> Telefon raqam</p>
                </div>
                <div class="col-md-3">
                    <h3><%= districts.length %></h3>
                    <p class="mb-0"><i class="bi bi-geo"></i> Tuman/Shahar</p>
                </div>
                <div class="col-md-3">
                    <h3><%= route.time_window_minutes %> daq</h3>
                    <p class="mb-0"><i class="bi bi-clock"></i> Vaqt oralig'i</p>
                </div>
                <div class="col-md-3">
                    <h3><%= totalMessages %></h3>
                    <p class="mb-0"><i class="bi bi-chat-dots"></i> E'lonlar</p>
                </div>
            </div>
        </div>

        <!-- Yo'nalish tanlov -->
        <div class="card mb-4">
            <div class="card-body">
                <h5 class="card-title"><i class="bi bi-signpost-2"></i> Yo'nalish</h5>
                <select class="form-select" id="directionSelect" onchange="changeDirection()">
                    <option value="from" selected><%= route.name %>dan → Toshkentga</option>
                    <option value="to">Toshkentdan → <%= route.name %>ga</option>
                </select>
            </div>
        </div>

        <!-- Tumanlar ro'yxati -->
        <h4 class="mb-3">Tumanlar bo'yicha</h4>
        <div class="list-group">
            <% districts.forEach((district, index) => { %>
                <a href="/routes/district/<%= route.id %>/<%= district.district_name %>?direction=from" 
                   class="list-group-item list-group-item-action district-card">
                    <div class="d-flex w-100 justify-content-between align-items-center">
                        <h5 class="mb-1">
                            <i class="bi bi-pin-map text-primary"></i>
                            <%= district.district_name.charAt(0).toUpperCase() + district.district_name.slice(1) %>
                        </h5>
                        <div>
                            <span class="badge bg-primary rounded-pill me-2">
                                <%= district.phone_count %> ta telefon
                            </span>
                            <i class="bi bi-arrow-right"></i>
                        </div>
                    </div>
                    <p class="mb-1 text-muted">
                        <%= district.district_name.charAt(0).toUpperCase() + district.district_name.slice(1) %> → Toshkent
                    </p>
                </a>
            <% }); %>
        </div>

        <% if (districts.length === 0) { %>
            <div class="alert alert-warning" role="alert">
                <i class="bi bi-exclamation-triangle"></i>
                Hozirda hech qanday e'lon topilmadi. Iltimos, keyinroq qayta urinib ko'ring.
